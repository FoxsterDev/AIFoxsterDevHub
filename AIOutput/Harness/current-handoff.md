# Current Unity Harness Handoff

Updated: 2026-08-31

- Current static contract: the exact nested MCP tag, package, peeled commit,
  and all seven consumer pins align. The root gate derives those facts from
  exact checked-in gitlinks, MCP package metadata/tag, and consumer
  manifests/locks. It owns no frozen release constant and calls no retired
  router generator.
- Frozen eval: 10/10 cases; 24/24 tracked tests; 12/12 built-in mutations
  rejected. The historical mandatory release failure remains blocked replay
  evidence, not a new live result.
- Routing: the public taxonomy preserves the unsupported legacy lane as
  non-release evidence and validates semantic standalone mode under the MCP's
  `## Mode Detection` contract.
- Stop hook: static-only, fail-open on hook faults, and scoped to Harness/router
  surfaces; ordinary product and `ConnectivityCheckerPro/Marketing/**` paths
  no-op. Owner trust through `/hooks` is still required.
- Proof ceiling for this update: static. Unity, package runtime, consumer
  compile, device, and release validation were not rerun for this tooling/docs
  change. Historical 2026-08-29 evidence remains historical.
- Detailed current evidence:
  `AIOutput/Harness/validation-evidence-2026-08-31.md`.
